name: bounce_tapper
description: "Smooth on-tap bounce (shrink/grow) animation that can easily enhance user touch interactions."
version: 1.0.5
homepage: https://github.com/Xim-ya/bounce_tapper

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
