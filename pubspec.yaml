name: zoom_tapper
description: "Smooth touch animations with Shrink/Grow effects, easily enhancing user interaction."
version: 1.0.0
homepage: https://github.com/Xim-ya/zoom_tapper

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
